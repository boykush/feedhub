#!/usr/bin/env bash
#MISE description="Build and load collector-migration image to kind cluster"
#MISE dir="server/collector"

IMAGE="collector-migration:latest"

echo "Building image: ${IMAGE}"
docker build -t "$IMAGE" -f Dockerfile.migrate .

echo "Loading image: ${IMAGE}"
kind load docker-image "${IMAGE}" --name feedhub

echo "Done!"
