#!/usr/bin/env bash
#MISE description="Build and load feed Docker image to kind cluster"

IMAGE="feed-service:latest"

echo "Building image: ${IMAGE}"
cd "services/feed" || exit 1
docker build -t "$IMAGE" .
cd - > /dev/null

echo "Loading image: ${IMAGE}"
kind load docker-image "${IMAGE}" --name foresee

echo "Done!"
