#!/usr/bin/env bash
#MISE description="Create cluster, build images, deploy, and start port-forwards"
#MISE depends=["k8s:local:cluster:create", "k8s:local:cluster:load-image", "k8s:local:deploy", "k8s:local:forward"]
